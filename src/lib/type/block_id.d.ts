export declare class block_id {
    data: Uint8Array;
    setData(data: Uint8Array): void;
    setDataSlice(data: Uint8Array, start: number): void;
    getData(): Uint8Array;
    blockNum(): number;
    toString(): string;
}
